# Use legacy peer dependency resolution to handle React Native/Expo peer dependency conflicts
# React Native ecosystem often has version mismatches that are safe in practice
# but fail npm's strict peer dependency checks. This is a common pattern for RN/Expo projects.
legacy-peer-deps=true
